using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Collections.Concurrent;
using System.Threading;

namespace Surface.MyTimer
{
    class BasicShow
    {
        /*
        private const int freq = 300;

        public void showP()
        {
            while (true)
            {
                Form1.label1.Text = "";                         
                Form1.label1.Text = Data.TargetValue.ToString();
                Form1.label2.Text = "";
                Form1.label2.Text = Data.HoldingValue.ToString();
                Form1.label3.Text = "";
                Form1.label3.Text = Data.IndexValue.ToString();
                Form1.label4.Text = "";
                Form1.label4.Text = Data.MinusValue.ToString();
                Thread.Sleep(freq);
            }
        }

        public void showD(ConcurrentDictionary<string, Data.StockInfo> Dict)
        {
            List<Data.StockInfo> DictionaryToList = Dict.Values.ToList();
            while (true)
            {
                
                Form1.dataGridViewHolding.DataSource = null;                             
                Form1.dataGridViewHolding.DataSource = dataSourceM;
                Thread.Sleep(freq);
            }
        }
        */
    }
    
}
